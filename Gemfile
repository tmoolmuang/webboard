source 'https://rubygems.org'

gem 'rails', '4.2.8'
gem 'sass-rails', '~> 5.0'
gem 'uglifier', '>= 1.3.0'
gem 'coffee-rails', '~> 4.1.0'
gem 'jquery-rails'
gem 'turbolinks'
gem 'jbuilder', '~> 2.0'
gem 'sdoc', '~> 0.4.0', group: :doc
gem 'bootstrap-sass'        # manually added
gem 'faker'                 # manually added
gem 'kaminari'              # manually added
gem 'devise'                # manually added

group :development, :test do
  gem 'sqlite3'             # manually added
  gem 'byebug'
  gem 'rspec-rails'         # manually added
  gem 'factory_girl_rails'  # manually added
  gem 'capybara'            # manually added
  gem 'spring'
  gem 'shoulda'             # manually added
end

group :development do
  gem 'web-console', '~> 2.0'
  gem "letter_opener"   # manually added
end

group :production do    # manually added
  gem 'pg'
  gem 'rails_12factor'  
end

group :test do
  gem 'database_cleaner'
end
